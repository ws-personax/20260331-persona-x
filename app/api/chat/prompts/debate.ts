// 재테크 탭 프롬프트 모음.
//
// 현재 재테크 탭의 페르소나 응답(RAY/JACK/LUCIA/ECHO)은
// route.ts 내부에서 상황별로 동적 조합되는 인라인 템플릿 문자열이며
// (예: 섹터 비교, 개장 전 브리핑, 디커플링 분석 등)
// 시장 데이터(`${krData?.price}`, `${usChange}` 등)와
// 계산 결과(`${signal}`, `${briefingText}` 등)에 직접 의존합니다.
//
// 공용 카피 문구(DISCLAIMER 등)는 이미 `@/lib/personax/scoring` 에
// 분리되어 있어 별도 추출이 불필요합니다.
//
// 추후 재테크 탭에 LLM 시스템 프롬프트가 도입되거나
// 동적 템플릿에서 정적 부분을 분리할 때 이 파일에 추가합니다.

export {};
